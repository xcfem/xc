# Verification tests when using two FE problems at once

The tests on this folder verfify that the program works fine when using two or more problems simultaneously. This is the case, for example, when you check the RC sections of the model using a phantom model (see postprocess/phantom_model.py) along with the main model.
